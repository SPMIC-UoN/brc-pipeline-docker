# Create qi python env and install basic dependencies
ARG REBUILD_ENV=1
COPY envs/mamba_env.yml /tmp/
RUN micromamba env create -n ukbb -f /tmp/mamba_env.yml && \
    micromamba clean -a -y && \
    echo "micromamba deactivate" >> ~/.bashrc && \
    echo "micromamba activate ukbb" >> ~/.bashrc

